# Components are copied standalone into other people's apps, so every import must
# be a declared dependency. No hoisting, not even into the virtual store.
hoist=false
strict-peer-dependencies=true
resolution-mode=highest
