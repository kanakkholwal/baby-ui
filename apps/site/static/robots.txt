# allow crawling everything by default; pages that shouldn't be indexed carry
# their own noindex meta tag instead, so crawlers can still see it
User-agent: *
Disallow:

Sitemap: https://baby-ui.nexonauts.com/sitemap.xml
