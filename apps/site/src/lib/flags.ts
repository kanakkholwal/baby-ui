/** React runner bundle URL enables live React previews. Null keeps React out of the
 * site build; the Preview tab then explains the substitution. */
export const REACT_RUNNER_URL: string | null = null;
