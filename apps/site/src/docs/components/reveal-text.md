---
title: Reveal Text
description: "Words or letters rise out of a blur one after another, on mount or in view."
component: reveal-text
category: text
tags: [text, reveal, stagger, scroll]
---

`trigger="view"` waits for an IntersectionObserver; with `once` off it replays on every
entry. Drive it yourself with `revealed`. Blur is skipped on touch screens.
