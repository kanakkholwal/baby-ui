---
title: Circular Text
description: Text arranged in a rotating circular path.
component: circular-text
category: text
tags: [text, circular, spin, rotate, logo]
---

The ring spins at a constant rate; each character's own placement around it is computed
once from the text length and stays static. `radius` also sizes the container, since a
ring inherently needs an explicit diameter rather than inheriting text flow.
