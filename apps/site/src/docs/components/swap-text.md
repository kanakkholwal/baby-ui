---
title: Swap Text
description: Swaps between two texts on click or hover.
component: swap-text
category: text
tags: [text, swap, toggle, hover, click]
---

Fully controlled: `active`/`defaultActive`/`onActiveChange`, the same triad every
component in this library uses. A real `<button>` underneath, so it's keyboard-reachable
even with `supportsHover` off.
