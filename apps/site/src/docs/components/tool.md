---
title: Tool Call
description: Collapsible tool invocation showing state, input and output.
component: tool
category: agents
tags: [tool]
---

Collapsed by default. A transcript where every tool call is expanded buries the answer
under its own plumbing.

State is a word next to the icon, not just a colour. Only the running state animates, so a
finished list of ten calls is completely still rather than a wall of spinners.
