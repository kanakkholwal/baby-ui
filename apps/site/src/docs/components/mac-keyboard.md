---
title: Mac Keyboard
description: A full MacBook keyboard whose keys press down with the physical keyboard or the pointer.
component: mac-keyboard
category: animated
tags: [keyboard, mac, keys, keycap]
---

Keys light up for the matching `KeyboardEvent.code` while the page has focus, and press under the
pointer too. The held keys are a controlled `pressed` array, so a demo can script a shortcut by
setting it.

Modifier legends and the accessible name come from `labels`.
