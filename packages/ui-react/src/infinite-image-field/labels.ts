/** Every announced string, overridable for other languages. */
export type InfiniteImageFieldLabels = { label: string };

export const INFINITE_IMAGE_FIELD_LABELS: InfiniteImageFieldLabels = {
	label: "Image field. Point away from the centre, or hold the arrow keys, to drift.",
};
