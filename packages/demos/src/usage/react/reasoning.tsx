import { Reasoning } from "@baby-ui/react";

export function Example() {
	return (
		<Reasoning thinking duration={4}>
			Checking the spec for both ports, then comparing the spring tables.
		</Reasoning>
	);
}
