import { Typography } from "@baby-ui/react";

export function Example() {
	return (
		<>
			<Typography variant="h2">One spec, two ports</Typography>
			<Typography variant="lead">The React and Svelte builds behave identically.</Typography>
			<Typography variant="muted">Neither is generated from the other.</Typography>
		</>
	);
}
